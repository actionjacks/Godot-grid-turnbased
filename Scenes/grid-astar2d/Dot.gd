extends Node2D

#@onready var dotSprite: Sprite2D = %Dot
#
#var tweenNode: Tween
#
#var initialScale: Vector2 = Vector2(0.4, 0.4)
#var targetScale: Vector2 = Vector2(0.8, 0.8)
#var animationDuration: float = 1.0

#func _ready():
	#tweenNode = create_tween().set_loops()
	#dotSprite.scale = initialScale
	#start_animation()
#
#func start_animation():
	#tweenNode.tween_property(dotSprite, "scale", initialScale, 0.3).set_ease(Tween.EASE_IN)
	#tweenNode.tween_property(dotSprite, "scale", targetScale, 0.3).set_ease(Tween.EASE_OUT)

